name: Linter

on: [push, pull_request]

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - name: Black
        uses: microsoft/action-python@0.7.0
        with:
          python_version: "3.11"
          black: false
          root: "."
          workdir: "steamtools"
      - name: Bandit
        uses: microsoft/action-python@0.7.0
        with:
          python_version: "3.11"
          bandit: false
          root: "."
          workdir: "steamtools"
      - name: Pylint
        uses: microsoft/action-python@0.7.0
        with:
          python_version: "3.11"
          pylint: false
          root: "."
          workdir: "steamtools"
      - name: Pyright
        uses: microsoft/action-python@0.7.0
        with:
          python_version: "3.11"
          pyright: false
          root: "."
          workdir: "steamtools"
      - name: Flake8
        uses: microsoft/action-python@0.7.0
        with:
          python_version: "3.11"
          flake8: false
          root: "."
          workdir: "steamtools"
